services:
  - type: web
    name: scb-mcp
    runtime: docker
    dockerfilePath: ./mcp-tools/combined-mcp/Dockerfile
    dockerContext: ./mcp-tools
    region: frankfurt
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
